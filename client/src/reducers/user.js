/**
 * This file holds reducers for the current user.
 */

/** The initial state of the reducer. */
const initState = {
  isLoggedOn: false,
  email: null
};

export default function(state = initState, action) {
  return state;
}
